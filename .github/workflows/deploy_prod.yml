name: Deploy Production
on:
  workflow_dispatch:
    inputs:
      node_version:
        description: 'Node.js version'
        required: false
        default: '22.18.0'
        type: string
  push:
    branches:
      - release

# 最小必要权限（根级），尽量收窄为后续步骤需要的权限
permissions:
  contents: write       # 推送 tag、创建 release、checkout/push 操作
  pages: write          # deploy-pages / upload-pages-artifact
  id-token: write       # deploy-pages 可能需要 OIDC
  pull-requests: write  # github-script 创建 PR

concurrency:
  group: deployment-prod-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # 预检步骤：在任何构建/部署前检测必须的 secrets/vars 是否存在，早失败并提示缺失项
  preflight:
    runs-on: ubuntu-latest
    outputs:
      ready: ${{ steps.check.outputs.ready }}
    environment:
      name: prod
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Check version updated for prod deployment
        run: |
          # Get current version from package.json
          CURRENT_VERSION=$(jq -r '.version' package.json)
          echo "Current version: $CURRENT_VERSION"

          # Get the latest tag version
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "none")
          echo "Latest tag: $LATEST_TAG"

          # Extract version from latest tag (assuming format like v1.2.3 or 1.2.3)
          if [ "$LATEST_TAG" != "none" ]; then
            LATEST_VERSION=$(echo "$LATEST_TAG" | sed 's/^v//' | grep -oE '^[0-9]+\.[0-9]+\.[0-9]+' || echo "0.0.0")
          else
            LATEST_VERSION="0.0.0"
          fi
          echo "Latest version: $LATEST_VERSION"

          # Compare versions using semantic versioning
          if [ "$CURRENT_VERSION" = "$LATEST_VERSION" ]; then
            echo "::error::Version in package.json ($CURRENT_VERSION) has not been updated. Please bump the version before deploying to prod."
            exit 1
          fi
          # Check if CURRENT_VERSION is greater than LATEST_VERSION
          if [ "$(printf '%s\n' "$LATEST_VERSION" "$CURRENT_VERSION" | sort -V | head -n1)" != "$LATEST_VERSION" ]; then
            echo "::error::Version in package.json ($CURRENT_VERSION) is less than the latest released version ($LATEST_VERSION). Please bump the version before deploying to prod."
            exit 1
          fi

          echo "✅ Version check passed: $LATEST_VERSION -> $CURRENT_VERSION"

      - name: Check required secrets and variables
        id: check
        run: |
          missing=""
          # Check SSH secret for server deploy
          if [ -z "${{ secrets.ALY_SSH_TOKEN }}" ]; then
            missing="${missing} secrets.ALY_SSH_TOKEN"
          fi

          # Check DEPLOY_SERVERS variable
          if [ -z "${{ vars.DEPLOY_SERVERS }}" ]; then
            missing="${missing} vars.DEPLOY_SERVERS"
          fi

          # If Pages deployment will run (only for workflow_dispatch + prod), we don't require extra secrets here.
          # Emit helpful guidance and fail early if any required items missing.
          if [ -n "$missing" ]; then
            echo "Missing required configuration:$missing"
            echo "::error::Missing required configuration:$missing"
            exit 1
          fi

          echo "All required secrets/vars present."
          echo "ready=true" >> $GITHUB_OUTPUT

  build:
    needs: preflight
    runs-on: ubuntu-latest
    env:
      NODE_VERSION: ${{ github.event_name == 'workflow_dispatch' && inputs.node_version || '22.18.0' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Cache node_modules
        uses: actions/cache@v5
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ env.NODE_VERSION }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install deps
        run: npm install

      - name: Build
        run: npm run build

      - name: Upload static files as artifact for Pages
        id: pages_artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: build/

      - name: Upload build artifact (for SSH deploy)
        uses: actions/upload-artifact@v7
        with:
          retention-days: 2
          name: build-dist
          path: build/

  prepare-deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: prod
    outputs:
      servers: ${{ steps.set-matrix.outputs.servers }}
    steps:
      - name: Set deploy servers matrix
        id: set-matrix
        env:
          SERVERS: ${{ vars.DEPLOY_SERVERS }}
        run: |
          echo "servers=$SERVERS" >> $GITHUB_OUTPUT

  deploy-pages:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5

  deploy:
    needs: prepare-deploy
    runs-on: [self-hosted, deploy-prod] # Use a dedicated label for secure deployment runner
    environment:
      name: prod
    permissions: # Scope permissions as narrowly as possible
      contents: read
      id-token: write
      actions: read
    strategy:
      fail-fast: true
      max-parallel: 2
      matrix:
        server: ${{ fromJson(needs.prepare-deploy.outputs.servers) }}
    steps:
      - name: Verify runner security and dependencies
        run: |
          echo "Verifying runner security and dependencies..."
          # Check for required tools
          which ssh || (echo "ssh not found" && exit 1)
          which rsync || (echo "rsync not found" && exit 1)
          # Optionally check for OS updates
          # sudo apt-get update -y
          # sudo apt-get upgrade -y
          echo "Runner verification complete."
      - name: Download build artifact for SSH deploy
        uses: actions/download-artifact@v8
        with:
          name: build-dist
          path: build

      - name: SSH deploy to server
        uses: easingthemes/ssh-deploy@v5.1.1
        env:
          REMOTE_USER: ${{ matrix.server.user }}
          REMOTE_HOST: ${{ matrix.server.host }}
          REMOTE_PORT: "22"
          ARGS: "-rltgoDzvO --delete"
          SSH_PRIVATE_KEY: ${{ secrets.ALY_SSH_TOKEN }}
          SOURCE: "build/*"
          TARGET: "/var/www/timeline"

  tag-and-release:
    needs: [deploy, deploy-pages]
    if: ${{ !cancelled() && needs.deploy.result == 'success' }}
    runs-on: ubuntu-latest
    env:
      NODE_VERSION: ${{ github.event_name == 'workflow_dispatch' && inputs.node_version || '22.18.0' }}
    steps:
      - name: Checkout code for tagging
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Create tag
        id: create_tag
        run: |
          set -e
          # Get version from package.json
          VERSION=$(jq -r '.version' package.json)
          echo "Version from package.json: $VERSION"

          # Create tag name with version
          TAG="v${VERSION}"
          echo "TAG_NAME=$TAG" >> $GITHUB_ENV
          echo "VERSION=$VERSION" >> $GITHUB_ENV

          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git tag "$TAG" "$GITHUB_SHA"
          git push origin "$TAG"

      - name: Setup Node for changelog
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Generate changelog
        id: gen_changelog
        run: |
          npx git-cliff --config git-cliff.toml --latest --strip header --no-exec -o CHANGELOG_TEMP.md
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          cat CHANGELOG_TEMP.md >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Download build artifact
        uses: actions/download-artifact@v8
        with:
          name: build-dist
          path: build-dist

      - name: Package build artifacts
        run: |
          cd build-dist
          tar -czf ../blog-build-${{ env.VERSION }}.tar.gz .
          zip -r ../blog-build-${{ env.VERSION }}.zip .
          cd ..

      - name: Create GitHub Release
        run: |
          gh release create "${{ env.TAG_NAME }}" \
            --title "Release ${{ env.VERSION }}" \
            --notes "${{ steps.gen_changelog.outputs.changelog }}" \
            --target "$GITHUB_SHA" \
            blog-build-${{ env.VERSION }}.tar.gz \
            blog-build-${{ env.VERSION }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create PR to merge into main
        uses: actions/github-script@v8
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const ref = context.ref || '';
            const branch = ref.replace('refs/heads/', '');
            if (!branch) {
              core.info('Cannot determine branch name from context.ref; skipping PR creation.');
              return;
            }
            const { data: existing } = await github.rest.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              head: `${context.repo.owner}:${branch}`,
              base: 'main'
            });
            if (existing.length) { core.info(`PR already exists: #${existing[0].number}`); return; }
            const { data: pr } = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head: branch,
              base: 'main',
              title: `chore: merge ${branch} into main (auto after deploy)`,
              body: 'Auto-created by CI after successful deploy.'
            });
            core.notice(`Created PR #${pr.number}`);
